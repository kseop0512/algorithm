-- 코드를 입력하세요

SELECT 
TBL.FLAVOR
FROM
(
SELECT
FH.FLAVOR
, SUM(FH.TOTAL_ORDER + JL.TOTAL_ORDER) AS TOTAL_OD
FROM FIRST_HALF FH
INNER JOIN JULY JL
ON JL.FLAVOR = FH.FLAVOR
GROUP BY FH.FLAVOR
ORDER BY TOTAL_OD DESC
LIMIT 3
) TBL


